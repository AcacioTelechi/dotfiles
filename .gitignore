# OS / editor cruft
.DS_Store
*.swp
*.swo
*~

# Backups
*.bak
*.backup
*.orig

# zsh runtime (never dotfiles)
.zsh_history
.zcompdump*

# tmux plugins (managed by tpm, not committed)
tmux/.config/tmux/plugins/
